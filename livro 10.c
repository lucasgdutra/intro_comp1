#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main(){
  /*Faça um programa que calcule e mostre a área de um quadrado. sabe-se que: A = lado * lado*/
  float lado, area;
  scanf("%f",&lado);
  area = lado * lado;
  printf("area %f\n",area );
}
